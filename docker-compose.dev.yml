name: dev-profile-service
#version: "3.9"

services:
  profile-service:
    container_name: dev-profile-service
    build: .

    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"

    env_file:
      - ${ENV_COMMON_PATH:-../../config/.env.common}
      - .env.staging

    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:4002/health"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 30s

    expose:
      - "4002"

    environment:
      LOG_ROOT: /var/log/projectshell

    volumes:
      - /home/deploy/logs:/var/log/projectshell

    restart: always

    networks:
      - gateway_dev-net

networks:
  gateway_dev-net:
    external: true
